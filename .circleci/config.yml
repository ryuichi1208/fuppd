version: 2.1
jobs:
  lint:
    machine: true
    steps:
      - checkout
      - run:
          name: start container
          command: docker version && docker image build -t app01 .

workflows:
  version: 2
  build_and_test:
    jobs:
      - lint
